{
  "name": "server",
  "version": "1.0.0",
  "description": "Server that implements CRUD for tab app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Naama Iluz",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.1"
  }
}
